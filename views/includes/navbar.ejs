

<style>
  body {
    background: linear-gradient(135deg, #0a0a0a, #1a1a1a);
    color: white;
    font-family: 'Segoe UI', sans-serif;
  }

  .navbar {
    background: linear-gradient(135deg, #1e1e1e, #333);
    padding: 1rem 2rem;
    box-shadow: 0 4px 10px rgba(255, 255, 255, 0.1);
  }

  .navbar-brand {
    font-size: 1.8rem;
    font-weight: bold;
    color: #ffffff;
    display: flex;
    align-items: center;
  }

  .navbar-brand img {
    height: 40px;
    margin-right: 10px;
  }

  .search-bar {
    border-radius: 25px;
    width: 320px;
    padding: 0.6rem 1rem;
    border: 2px solid #ddd;
    transition: border 0.3s ease, box-shadow 0.3s ease;
  }

  .search-bar:focus {
    border-color: #ffcc00;
    outline: none;
    box-shadow: 0 0 10px rgba(255, 204, 0, 0.7);
  }

  .btn-search {
    background: linear-gradient(135deg, #ffcc00, #ff8800);
    color: #fff;
    border-radius: 25px;
    padding: 0.6rem 1.5rem;
    font-size: 1rem;
    font-weight: 500;
    border: none;
    transition: background 0.3s ease, transform 0.2s;
  }

  .btn-search:hover {
    background: linear-gradient(135deg, #ffbb00, #ff6600);
    transform: scale(1.1);
  }

  .nav-link {
    font-size: 1rem;
    font-weight: 500;
    transition: color 0.3s ease, text-shadow 0.3s ease;
    color: #ffffff !important;
    text-decoration: none;
    position: relative;
    animation: glowText 1.5s infinite alternate;
  }

  .nav-link:hover {
    color: #ffcc00;
    text-shadow: 0px 0px 10px rgba(255, 204, 0, 0.7);
  }

  .nav-link::after {
    content: '';
    position: absolute;
    width: 100%;
    height: 2px;
    bottom: -2px;
    left: 0;
    background: linear-gradient(90deg, #ffcc00, #ff8800);
    transform: scaleX(0);
    transition: transform 0.3s ease-in-out;
  }

  .nav-link:hover::after {
    transform: scaleX(1);
  }

  @keyframes glowText {
    0% { text-shadow: 0px 0px 5px rgba(255, 255, 255, 0.2); }
    100% { text-shadow: 0px 0px 15px rgba(255, 255, 255, 0.6); }
  }
</style>

<nav class="navbar navbar-expand-md border-bottom">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">
      <i class="fa-brands fa-think-peaks"> </i>  IndeHaven
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="mx-auto">
        <form class="d-flex" role="search" onsubmit="return searchFunction(event)">
          <input id="searchInput" class="form-control me-2 search-bar" type="search" placeholder="Search destinations" aria-label="Search">
          <button class="btn btn-outline-primary" type="submit" style="border-radius: 1rem;">
            <i class="fa-solid fa-magnifying-glass"></i> Search
          </button>
        </form>
      </div>
      <div class="navbar-nav ms-auto d-flex align-items-center">
        <a class="nav-link" href="/listings/new">Become a Host</a>
        <% if (!currUser) { %>
          <a class="nav-link" href="/signup"><b>Sign Up</b></a>
          <a class="nav-link" href="/login"><b>Log In</b></a>
        <% } %>
        <% if (currUser) { %>
          <a class="nav-link" href="/logout"><b>Log Out</b></a>
          
        <% } %>
      </div>
    </div>
  </div>
</nav>

<div class="container text-center">
  <h1>Welcome to Our Services</h1>
  <p>Experience the best features with our platform.</p>
 
</div>

<script>
  function searchFunction(event) {
    event.preventDefault();
    let query = document.getElementById("searchInput").value.trim();
    if (query) {
      window.location.href = `/search?query=${encodeURIComponent(query)}`;
    }
  }
</script>
